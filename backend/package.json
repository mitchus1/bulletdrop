{
  "name": "bulletdrop-backend",
  "version": "1.0.0",
  "description": "FastAPI backend for Discord profile and image hosting",
  "scripts": {
    "dev": "../.venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "../.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "../.venv/bin/pytest",
    "migrate": "../.venv/bin/alembic upgrade head",
    "makemigrations": "../.venv/bin/alembic revision --autogenerate"
  },
  "keywords": ["fastapi", "python", "api"],
  "author": "",
  "license": "MIT"
}